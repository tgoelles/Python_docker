name: base
channels:
  - conda-forge
  - defaults
dependencies:
  - pandas
  - numpy
  - pip
  - python
  - streamlit
  - conda
  - psutil
  - xlrd
  - pip:
      - plotly
      - mypy
      - flake8
      - black
      - pylint
      - rich
      - isort
      - coverage
      - pytest
      - pytest-check
      - requests
      - matplotlib
      - docker-py
prefix: /opt/conda
