name: base
channels:
  - conda-forge
  - defaults
dependencies:
  - pandas
  - numpy
  - pip
  - python>=3.11,<3.12
  - conda
  - psutil
  - xlrd
  - ipykernel
  - pip:
      - plotly
      - kaleido
      - mypy
      - ruff
      - black
      - rich
      - rich-cli
      - coverage
      - pytest
      - pytest-check
      - jupyterlab
prefix: /opt/conda
